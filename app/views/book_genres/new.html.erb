<%= form_for(@book_genre) do |f| %>
    <% if @book_genre.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@book_genre.errors.count, "error") %> prohibited from being saved:</h2>

          <ul>
            <% @book_genre.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :book_id %>
      <%= f.select :book_id, options_from_collection_for_select(@books, :id, :name, @book_id) %><br>
    </div>
    <div class="field">
      <%= f.label :genre_id %>
      <%= f.select :genre_id, options_from_collection_for_select(@genres, :id, :name) %><br>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
